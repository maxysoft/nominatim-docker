name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: Build docker image

    runs-on: ubuntu-latest
    outputs:
      NOMINATIM_VERSION: ${{ steps.get_version.outputs.NOMINATIM_VERSION }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract Nominatim Version
        id: get_version
        run: echo "NOMINATIM_VERSION=$(grep -oP '(?<=ARG NOMINATIM_VERSION=)[0-9.]+' Dockerfile)" >> $GITHUB_OUTPUT

      - name: Build and export image
        uses: docker/build-push-action@v5
        with:
          context: .
          tags: nominatim:${{ steps.get_version.outputs.NOMINATIM_VERSION }}
          outputs: type=docker,dest=/tmp/nominatim.tar

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: nominatim-${{ steps.get_version.outputs.NOMINATIM_VERSION }}
          path: /tmp/nominatim.tar

  test:
    name: Test docker image
    needs: build
    runs-on: ubuntu-latest
    env:
      NOMINATIM_VERSION: ${{ needs.build.outputs.NOMINATIM_VERSION }}
      POSTGRES_PASSWORD: very_secure_password

    strategy:
      matrix:
        test_scenario:
          - name: Import with PBF_URL and update
            commands: |-
              # Start PostgreSQL with PostGIS
              docker network create nominatim-net
              docker run -d --rm --name postgres --network nominatim-net \
                -e POSTGRES_PASSWORD=very_secure_password \
                -e POSTGRES_USER=postgres \
                -e POSTGRES_DB=nominatim \
                postgis/postgis:16-3.4
              
              # Wait for PostgreSQL to be ready
              sleep 10
              
              # get the data from four days ago to make sure there really are updates to apply
              four_days_ago=`date --date="4 days ago" +%y%m%d`
              docker run -i --rm --network nominatim-net \
                -e POSTGRES_HOST=postgres \
                -e POSTGRES_PORT=5432 \
                -e POSTGRES_DB=nominatim \
                -e NOMINATIM_PASSWORD=very_secure_password \
                -e POSTGRES_ADMIN_PASSWORD=very_secure_password \
                -e PBF_URL=http://download.geofabrik.de/europe/monaco-${four_days_ago}.osm.pbf \
                -e REPLICATION_URL=http://download.geofabrik.de/europe/monaco-updates/ \
                -v nominatim-data:/nominatim \
                -p 8001:8080 \
                --name nominatim \
                nominatim:$NOMINATIM_VERSION &

              ./assert-non-empty-json "http://localhost:8001/search.php?q=avenue%20pasteur"
              docker exec -i nominatim sudo -u nominatim nominatim replication --project-dir /nominatim --once
              ./assert-non-empty-json "http://localhost:8001/search.php?q=avenue%20pasteur"
              
              # Cleanup
              docker stop nominatim postgres || true
              docker network rm nominatim-net || true
          
          - name: Import with volume mount
            commands: |-
              # Start PostgreSQL with PostGIS
              docker network create nominatim-net2
              docker run -d --rm --name postgres --network nominatim-net2 \
                -e POSTGRES_PASSWORD=very_secure_password \
                -e POSTGRES_USER=postgres \
                -e POSTGRES_DB=nominatim \
                postgis/postgis:16-3.4
              
              sleep 10
              
              docker run -i --rm --network nominatim-net2 \
                -e POSTGRES_HOST=postgres \
                -e POSTGRES_PORT=5432 \
                -e POSTGRES_DB=nominatim \
                -e NOMINATIM_PASSWORD=very_secure_password \
                -e POSTGRES_ADMIN_PASSWORD=very_secure_password \
                -e PBF_URL=http://download.geofabrik.de/europe/monaco-latest.osm.pbf \
                -e REPLICATION_URL=http://download.geofabrik.de/europe/monaco-updates/ \
                -v nominatim-data2:/nominatim \
                -p 8002:8080 \
                nominatim:$NOMINATIM_VERSION &

              ./assert-non-empty-json "http://localhost:8002/search.php?q=avenue%20pasteur"
              
              # Cleanup
              docker stop postgres || true
              docker network rm nominatim-net2 || true
          
          - name: Import with persistent data and container restart
            commands: |-
              # Start PostgreSQL with PostGIS with persistent volume
              docker network create nominatim-net3
              docker run -d --rm --name postgres --network nominatim-net3 \
                -e POSTGRES_PASSWORD=very_secure_password \
                -e POSTGRES_USER=postgres \
                -e POSTGRES_DB=nominatim \
                -v postgres-data:/var/lib/postgresql/data \
                postgis/postgis:16-3.4
              
              sleep 10
              
              # get the data from four days ago to make sure there really are updates to apply
              four_days_ago=`date --date="4 days ago" +%y%m%d`
              docker run -i --rm --network nominatim-net3 \
                -e POSTGRES_HOST=postgres \
                -e POSTGRES_PORT=5432 \
                -e POSTGRES_DB=nominatim \
                -e NOMINATIM_PASSWORD=very_secure_password \
                -e POSTGRES_ADMIN_PASSWORD=very_secure_password \
                -e PBF_URL=http://download.geofabrik.de/europe/monaco-${four_days_ago}.osm.pbf \
                -e REPLICATION_URL=http://download.geofabrik.de/europe/monaco-updates/ \
                -v nominatim-data3:/nominatim \
                -p 8003:8080 \
                --name nominatim \
                nominatim:$NOMINATIM_VERSION &

              ./assert-non-empty-json "http://localhost:8003/search.php?q=avenue%20pasteur"
              
              # Stop containers
              docker stop nominatim
              # the container needs a few seconds to shutdown cleanly apparently
              sleep 5

              # Start nominatim again with same database
              docker run -i --rm --network nominatim-net3 \
                -e POSTGRES_HOST=postgres \
                -e POSTGRES_PORT=5432 \
                -e POSTGRES_DB=nominatim \
                -e NOMINATIM_PASSWORD=very_secure_password \
                -e POSTGRES_ADMIN_PASSWORD=very_secure_password \
                -e PBF_URL=http://download.geofabrik.de/europe/monaco-${four_days_ago}.osm.pbf \
                -e REPLICATION_URL=http://download.geofabrik.de/europe/monaco-updates/ \
                -v nominatim-data3:/nominatim \
                -p 8004:8080 \
                --name nominatim \
                nominatim:$NOMINATIM_VERSION &

              ./assert-non-empty-json "http://localhost:8004/search.php?q=avenue%20pasteur"
              docker exec -i nominatim sudo -u nominatim nominatim replication --project-dir /nominatim --once
              ./assert-non-empty-json "http://localhost:8004/search.php?q=avenue%20pasteur"
              
              # Cleanup
              docker stop nominatim postgres || true
              docker network rm nominatim-net3 || true

          - name: UPDATE_MODE=once
            commands: |-
              # Start PostgreSQL with PostGIS
              docker network create nominatim-net-update1
              docker run -d --rm --name postgres --network nominatim-net-update1 \
                -e POSTGRES_PASSWORD=very_secure_password \
                -e POSTGRES_USER=postgres \
                -e POSTGRES_DB=nominatim \
                -v postgres-data-update1:/var/lib/postgresql/data \
                postgis/postgis:16-3.4
              
              sleep 10
              
              # get the data from four days ago to make sure there really are updates to apply
              four_days_ago=`date --date="4 days ago" +%y%m%d`
              docker run -i --rm --network nominatim-net-update1 \
                -e POSTGRES_HOST=postgres \
                -e POSTGRES_PORT=5432 \
                -e POSTGRES_DB=nominatim \
                -e NOMINATIM_PASSWORD=very_secure_password \
                -e POSTGRES_ADMIN_PASSWORD=very_secure_password \
                -e PBF_URL=http://download.geofabrik.de/europe/monaco-${four_days_ago}.osm.pbf \
                -e REPLICATION_URL=http://download.geofabrik.de/europe/monaco-updates/ \
                -e UPDATE_MODE=once \
                -v nominatim-data-update1:/nominatim \
                -p 8020:8080 \
                --name nominatim \
                nominatim:$NOMINATIM_VERSION &

              ./assert-non-empty-json "http://localhost:8020/search.php?q=avenue%20pasteur"
              echo -n "check replication log for Update completed. Count:"
              docker exec nominatim /bin/bash -c 'tail -f /var/log/replication.log | grep -m 1 -c "Update completed." '
              
              # Cleanup
              docker stop nominatim postgres || true
              docker network rm nominatim-net-update1 || true

          - name: UPDATE_MODE=continuous
            commands: |-
              # Start PostgreSQL with PostGIS
              docker network create nominatim-net-update2
              docker run -d --rm --name postgres --network nominatim-net-update2 \
                -e POSTGRES_PASSWORD=very_secure_password \
                -e POSTGRES_USER=postgres \
                -e POSTGRES_DB=nominatim \
                -v postgres-data-update2:/var/lib/postgresql/data \
                postgis/postgis:16-3.4
              
              sleep 10
              
              # get the data from few days ago to make sure there really are updates to apply
              days_ago=`date --date="4 days ago" +%y%m%d`
              docker run -i --rm --network nominatim-net-update2 \
                -e POSTGRES_HOST=postgres \
                -e POSTGRES_PORT=5432 \
                -e POSTGRES_DB=nominatim \
                -e NOMINATIM_PASSWORD=very_secure_password \
                -e POSTGRES_ADMIN_PASSWORD=very_secure_password \
                -e PBF_URL=http://download.geofabrik.de/europe/monaco-${days_ago}.osm.pbf \
                -e REPLICATION_URL=http://download.geofabrik.de/europe/monaco-updates/ \
                -e UPDATE_MODE=continuous \
                -v nominatim-data-update2:/nominatim \
                -p 8021:8080 \
                --name nominatim \
                nominatim:$NOMINATIM_VERSION &

              ./assert-non-empty-json "http://localhost:8021/search.php?q=avenue%20pasteur"
              echo -n "check replication log for Update completed. Count:"
              docker exec nominatim /bin/bash -c 'tail -f /var/log/replication.log | grep -m 1 -c "Update completed." '
              
              # Cleanup
              docker stop nominatim postgres || true
              docker network rm nominatim-net-update2 || true

          - name: Import full style
            commands: |-
              # Start PostgreSQL with PostGIS
              docker network create nominatim-net4
              docker run -d --rm --name postgres --network nominatim-net4 \
                -e POSTGRES_PASSWORD=very_secure_password \
                -e POSTGRES_USER=postgres \
                -e POSTGRES_DB=nominatim \
                postgis/postgis:16-3.4
              
              sleep 10
              
              docker run -i --rm --network nominatim-net4 \
                -e POSTGRES_HOST=postgres \
                -e POSTGRES_PORT=5432 \
                -e POSTGRES_DB=nominatim \
                -e NOMINATIM_PASSWORD=very_secure_password \
                -e POSTGRES_ADMIN_PASSWORD=very_secure_password \
                -e PBF_URL=http://download.geofabrik.de/europe/monaco-latest.osm.pbf \
                -e REPLICATION_URL=http://download.geofabrik.de/europe/monaco-updates/ \
                -e IMPORT_STYLE=full \
                -v nominatim-data4:/nominatim \
                -p 8005:8080 \
                nominatim:$NOMINATIM_VERSION &

              ./assert-non-empty-json "http://localhost:8005/search.php?q=hotel%20de%20paris"
              
              # Cleanup
              docker stop postgres || true
              docker network rm nominatim-net4 || true
          
          - name: Import admin style
            commands: |-
              # Start PostgreSQL with PostGIS
              docker network create nominatim-net5
              docker run -d --rm --name postgres --network nominatim-net5 \
                -e POSTGRES_PASSWORD=very_secure_password \
                -e POSTGRES_USER=postgres \
                -e POSTGRES_DB=nominatim \
                postgis/postgis:16-3.4
              
              sleep 10
              
              docker run -i --rm --network nominatim-net5 \
                -e POSTGRES_HOST=postgres \
                -e POSTGRES_PORT=5432 \
                -e POSTGRES_DB=nominatim \
                -e NOMINATIM_PASSWORD=very_secure_password \
                -e POSTGRES_ADMIN_PASSWORD=very_secure_password \
                -e PBF_URL=http://download.geofabrik.de/europe/monaco-latest.osm.pbf \
                -e REPLICATION_URL=http://download.geofabrik.de/europe/monaco-updates/ \
                -e IMPORT_STYLE=admin \
                -v nominatim-data5:/nominatim \
                -p 8006:8080 \
                nominatim:$NOMINATIM_VERSION &

              ./assert-empty-json "http://localhost:8006/search.php?q=hotel%20de%20paris"
              
              # Cleanup
              docker stop postgres || true
              docker network rm nominatim-net5 || true

          - name: Import with PBF_PATH
            commands: |-
              # Start PostgreSQL with PostGIS
              docker network create nominatim-net6
              docker run -d --rm --name postgres --network nominatim-net6 \
                -e POSTGRES_PASSWORD=very_secure_password \
                -e POSTGRES_USER=postgres \
                -e POSTGRES_DB=nominatim \
                postgis/postgis:16-3.4
              
              sleep 10
              
              wget --cut-dirs=1 -nH -xP /tmp/data http://download.geofabrik.de/europe/monaco-latest.osm.pbf
              docker run -i --rm --network nominatim-net6 \
                -e POSTGRES_HOST=postgres \
                -e POSTGRES_PORT=5432 \
                -e POSTGRES_DB=nominatim \
                -e NOMINATIM_PASSWORD=very_secure_password \
                -e POSTGRES_ADMIN_PASSWORD=very_secure_password \
                -e PBF_PATH=/nominatim/data/monaco-latest.osm.pbf \
                -e REPLICATION_URL=http://download.geofabrik.de/europe/monaco-updates/ \
                -v nominatim-data6:/nominatim \
                -v /tmp/data:/nominatim/data \
                -p 8007:8080 \
                --name nominatim \
                nominatim:$NOMINATIM_VERSION &

              ./assert-non-empty-json "http://localhost:8007/search.php?q=avenue%20pasteur"
              
              # Cleanup
              docker stop nominatim postgres || true
              docker network rm nominatim-net6 || true

          - name: REPLICATION_URL is blank
            commands: |-
              # Start PostgreSQL with PostGIS
              docker network create nominatim-net7
              docker run -d --rm --name postgres --network nominatim-net7 \
                -e POSTGRES_PASSWORD=very_secure_password \
                -e POSTGRES_USER=postgres \
                -e POSTGRES_DB=nominatim \
                postgis/postgis:16-3.4
              
              sleep 10
              
              docker run -i --rm --network nominatim-net7 \
                -e POSTGRES_HOST=postgres \
                -e POSTGRES_PORT=5432 \
                -e POSTGRES_DB=nominatim \
                -e NOMINATIM_PASSWORD=very_secure_password \
                -e POSTGRES_ADMIN_PASSWORD=very_secure_password \
                -e PBF_URL=http://download.geofabrik.de/europe/monaco-latest.osm.pbf \
                -v nominatim-data7:/nominatim \
                -p 8008:8080 \
                nominatim:$NOMINATIM_VERSION &

              ./assert-non-empty-json "http://localhost:8008/search.php?q=avenue%20pasteur"
              
              # Cleanup
              docker stop postgres || true
              docker network rm nominatim-net7 || true

          - name: FREEZE
            commands: |-
              # Start PostgreSQL with PostGIS
              docker network create nominatim-net8
              docker run -d --rm --name postgres --network nominatim-net8 \
                -e POSTGRES_PASSWORD=very_secure_password \
                -e POSTGRES_USER=postgres \
                -e POSTGRES_DB=nominatim \
                postgis/postgis:16-3.4
              
              sleep 10
              
              docker run -i --rm --network nominatim-net8 \
                -e POSTGRES_HOST=postgres \
                -e POSTGRES_PORT=5432 \
                -e POSTGRES_DB=nominatim \
                -e NOMINATIM_PASSWORD=very_secure_password \
                -e POSTGRES_ADMIN_PASSWORD=very_secure_password \
                -e PBF_URL=http://download.geofabrik.de/europe/monaco-latest.osm.pbf \
                -e FREEZE="true" \
                -v nominatim-data8:/nominatim \
                -p 8009:8080 \
                nominatim:$NOMINATIM_VERSION &

              ./assert-non-empty-json "http://localhost:8009/search.php?q=avenue%20pasteur"
              
              # Cleanup
              docker stop postgres || true
              docker network rm nominatim-net8 || true

          - name: GB postcode import
            commands: |-
              # Start PostgreSQL with PostGIS
              docker network create nominatim-net9
              docker run -d --rm --name postgres --network nominatim-net9 \
                -e POSTGRES_PASSWORD=very_secure_password \
                -e POSTGRES_USER=postgres \
                -e POSTGRES_DB=nominatim \
                postgis/postgis:16-3.4
              
              sleep 10
              
              docker run -i --rm --network nominatim-net9 \
                -e POSTGRES_HOST=postgres \
                -e POSTGRES_PORT=5432 \
                -e POSTGRES_DB=nominatim \
                -e NOMINATIM_PASSWORD=very_secure_password \
                -e POSTGRES_ADMIN_PASSWORD=very_secure_password \
                -e PBF_URL=https://download.geofabrik.de/europe/united-kingdom/england/rutland-latest.osm.pbf \
                -e IMPORT_GB_POSTCODES="true" \
                -v nominatim-data9:/nominatim \
                -p 8010:8080 \
                nominatim:$NOMINATIM_VERSION &

              ./assert-non-empty-json "http://localhost:8010/search.php?postalcode=LE15+8TX"
              ./assert-non-empty-json "http://localhost:8010/search.php?postalcode=PE9+3SY"
              ./assert-non-empty-json "http://localhost:8010/search.php?postalcode=PE9+4ES"
              
              # Cleanup
              docker stop postgres || true
              docker network rm nominatim-net9 || true
  
          - name: REVERSE_ONLY
            commands: |-
              # Start PostgreSQL with PostGIS
              docker network create nominatim-net10
              docker run -d --rm --name postgres --network nominatim-net10 \
                -e POSTGRES_PASSWORD=very_secure_password \
                -e POSTGRES_USER=postgres \
                -e POSTGRES_DB=nominatim \
                postgis/postgis:16-3.4
              
              sleep 10
              
              docker run -i --rm --network nominatim-net10 \
                -e POSTGRES_HOST=postgres \
                -e POSTGRES_PORT=5432 \
                -e POSTGRES_DB=nominatim \
                -e NOMINATIM_PASSWORD=very_secure_password \
                -e POSTGRES_ADMIN_PASSWORD=very_secure_password \
                -e PBF_URL=http://download.geofabrik.de/europe/monaco-latest.osm.pbf \
                -e REVERSE_ONLY="true" \
                -v nominatim-data10:/nominatim \
                -p 8011:8080 \
                nominatim:$NOMINATIM_VERSION &

              ./assert-non-empty-json "http://localhost:8011/reverse.php?lat=43.734&lon=7.42&format=jsonv2"
              
              # Cleanup
              docker stop postgres || true
              docker network rm nominatim-net10 || true
          
          - name: Check for clean shutdown
            commands: |-
              # Start PostgreSQL with PostGIS
              docker network create nominatim-net11
              docker run -d --rm --name postgres --network nominatim-net11 \
                -e POSTGRES_PASSWORD=very_secure_password \
                -e POSTGRES_USER=postgres \
                -e POSTGRES_DB=nominatim \
                postgis/postgis:16-3.4
              
              sleep 10
              
              docker run --detach --network nominatim-net11 \
                -e POSTGRES_HOST=postgres \
                -e POSTGRES_PORT=5432 \
                -e POSTGRES_DB=nominatim \
                -e NOMINATIM_PASSWORD=very_secure_password \
                -e POSTGRES_ADMIN_PASSWORD=very_secure_password \
                -e PBF_URL=http://download.geofabrik.de/europe/monaco-latest.osm.pbf \
                -v nominatim-data11:/nominatim \
                -p 8012:8080 \
                --name nominatim \
                nominatim:$NOMINATIM_VERSION
            
              ./assert-non-empty-json "http://localhost:8012/search.php?q=avenue%20pasteur"
              # Shutdown the container
              docker stop -t 60 nominatim
              # Verify that the exit code is zero
              CONTAINER_EXIT_CODE=$(docker inspect nominatim --format='{{.State.ExitCode}}')
              test "$CONTAINER_EXIT_CODE" -eq 0
              
              # Cleanup
              docker rm nominatim || true
              docker stop postgres || true
              docker network rm nominatim-net11 || true

          - name: WARMUP_ON_STARTUP=true
            commands: |-
              # Start PostgreSQL with PostGIS
              docker network create nominatim-net12
              docker run -d --rm --name postgres --network nominatim-net12 \
                -e POSTGRES_PASSWORD=very_secure_password \
                -e POSTGRES_USER=postgres \
                -e POSTGRES_DB=nominatim \
                postgis/postgis:16-3.4
              
              sleep 10
              
              docker run -i --rm --network nominatim-net12 \
                -e POSTGRES_HOST=postgres \
                -e POSTGRES_PORT=5432 \
                -e POSTGRES_DB=nominatim \
                -e NOMINATIM_PASSWORD=very_secure_password \
                -e POSTGRES_ADMIN_PASSWORD=very_secure_password \
                -e PBF_URL=http://download.geofabrik.de/europe/monaco-latest.osm.pbf \
                -e WARMUP_ON_STARTUP=true \
                -v nominatim-data12:/nominatim \
                -p 8013:8080 \
                --name nominatim \
                nominatim:$NOMINATIM_VERSION &

              ./assert-non-empty-json "http://localhost:8013/search.php?q=avenue%20pasteur"
              docker logs nominatim | grep "Warming finished"
              
              # Cleanup
              docker stop nominatim postgres || true
              docker network rm nominatim-net12 || true

          - name: WARMUP_ON_STARTUP=false
            commands: |-
              # Start PostgreSQL with PostGIS
              docker network create nominatim-net13
              docker run -d --rm --name postgres --network nominatim-net13 \
                -e POSTGRES_PASSWORD=very_secure_password \
                -e POSTGRES_USER=postgres \
                -e POSTGRES_DB=nominatim \
                postgis/postgis:16-3.4
              
              sleep 10
              
              docker run -i --rm --network nominatim-net13 \
                -e POSTGRES_HOST=postgres \
                -e POSTGRES_PORT=5432 \
                -e POSTGRES_DB=nominatim \
                -e NOMINATIM_PASSWORD=very_secure_password \
                -e POSTGRES_ADMIN_PASSWORD=very_secure_password \
                -e PBF_URL=http://download.geofabrik.de/europe/monaco-latest.osm.pbf \
                -e WARMUP_ON_STARTUP=false \
                -v nominatim-data13:/nominatim \
                -p 8014:8080 \
                --name nominatim \
                nominatim:$NOMINATIM_VERSION &

              ./assert-non-empty-json "http://localhost:8014/search.php?q=avenue%20pasteur"
              docker logs nominatim | grep "Skipping cache warmup"
              
              # Cleanup
              docker stop postgres || true
              docker network rm nominatim-net13 || true
    
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: nominatim-${{ needs.build.outputs.NOMINATIM_VERSION }}
          path: /tmp
      - name: Load image
        run: |
          docker load --input /tmp/nominatim.tar
          docker image ls -a
      - name: Checkout
        uses: actions/checkout@v4

      # Excute the test scenario
      - name: ${{ matrix.test_scenario.name }}
        working-directory: .github/workflows
        run: ${{ matrix.test_scenario.commands }}
  
  publish:
    name: Publish docker image
    needs: test
    runs-on: ubuntu-latest
    # Only run on push to master branch
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Get commit short reference
        id: get_commit
        run: echo "SHORT_SHA=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      - name: Extract Nominatim Version
        id: get_version
        run: echo "NOMINATIM_VERSION=$(grep -oP '(?<=ARG NOMINATIM_VERSION=)[0-9.]+' Dockerfile)" >> $GITHUB_OUTPUT

      # Verify Dockerfile exists and get build info
      - name: Verify Dockerfile
        run: |
          test -f Dockerfile
          echo "Dockerfile found ✅"
          echo "Nominatim version: ${{ steps.get_version.outputs.NOMINATIM_VERSION }}"
          echo "Commit SHA: ${{ steps.get_commit.outputs.SHORT_SHA }}"

      - name: Login to Docker Hub
        if: ${{ github.repository_owner == 'maxysoft' }}
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push to Docker Hub
        if: ${{ github.repository_owner == 'maxysoft' }}
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ github.repository }}:v${{ steps.get_version.outputs.NOMINATIM_VERSION }}-${{ steps.get_commit.outputs.SHORT_SHA }}
            ${{ github.repository }}:latest
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.created=${{ github.event.head_commit.timestamp }}
            org.opencontainers.image.version=${{ steps.get_version.outputs.NOMINATIM_VERSION }}

      - name: Build and push to GitHub Container Registry
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:v${{ steps.get_version.outputs.NOMINATIM_VERSION }}-${{ steps.get_commit.outputs.SHORT_SHA }}
            ghcr.io/${{ github.repository }}:latest
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.created=${{ github.event.head_commit.timestamp }}
            org.opencontainers.image.version=${{ steps.get_version.outputs.NOMINATIM_VERSION }}
